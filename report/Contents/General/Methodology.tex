%%%
%
% $Autor: Siddhanth Sharma, Shubhankar Dumka, Gautam Ramesh $
% $Datum: 2025-12-01 $
% $Dateiname: 
% $Version: 4620 $
%
% !TeX spellcheck = GB
% !TeX program = pdflatex
% !TeX encoding = utf8
%
%%%

\chapter{Methodology}

\index{Methodology}

% --------------------------------------------------------
\section{Introduction}
\index{Methodology!Introduction}

Traditionally, ANPR pipelines consisted of separate modules for localization, segmentation, and recognition, often relying on handcrafted features and heuristic rules \cite{turn0search28}. With the advent of deep learning, unified approaches that leverage Convolutional Neural Networks (CNNs) and end-to-end object detectors such as YOLO have demonstrated significantly improved robustness and accuracy in complex environmental conditions \cite{turn0search5,turn0search0}.\newline 

Building and evaluating a license plate detection system requires a holistic methodological framework to connect low-level image data with actionable outcomes such as recognized license text, timestamped events, and billing information. While many deep learning applications focus narrowly on model training or post-deployment analytics, the Knowledge Discovery in Databases (KDD) process offers a broader, structured paradigm for transforming raw data into high-level knowledge through iterative stages of selection, preprocessing, transformation, mining, interpretation, and presentation \cite{fayyad1996knowledge}. Although KDD is a classic framework rooted in data mining and database research, its stages closely align with modern data science workflows and can be applied to both the training and operational phases of embedded computer vision systems.\newline

By adopting the KDD process, this work presents a systematic description of an license plate detection system that spans from data acquisition and model learning to deployment and real-time knowledge extraction on Jetson Nano. This end-to-end perspective highlights how data is progressively refined and interpreted, culminating in reliable license plate recognition and parking fee computation without reliance on external infrastructure. The following section elaborates on the methodology by detailing how each KDD stage is concretely realized within the proposed system.\newline

\section{Methodology: Knowledge Discovery in Databases (KDD)}
\index{Methodology!KDD}

\noindent
The Knowledge Discovery in Databases (KDD) process provides a structured framework for transforming raw data into meaningful and actionable knowledge through a sequence of well-defined stages \cite{fayyad1996knowledge}. In this project, KDD is applied holistically across both the \textit{model training phase} and the \textit{operational deployment phase}. Unlike methodologies that focus solely on post-deployment analytics, this consolidated KDD approach captures how knowledge is progressively extracted from raw visual data, beginning with dataset construction and model learning, and culminating in real-time license plate analysis and fee computation on embedded hardware.

\begin{figure}[htbp]
	\centering
	\input{../../tikz/6DomainKnowledgeMethodology/KDDVertical.tikz}
	\caption{Representation of the Knowledge Discovery in Databases (KDD) process applied to both training and deployment stages of the license plate detection system.}
	\label{fig:kdd_vertical_refined}
\end{figure}
 


\subsection{Data Selection}

The KDD process begins with data selection, where relevant data sources are identified and collected. For the training phase, a dataset consisting of 1,243 labeled images containing German vehicle license plates was selected. These images represent diverse environmental conditions, including variations in illumination, plate orientation, distance, and background complexity. Such diversity is critical for learning robust visual patterns in automatic license plate recognition (ALPR) systems \cite{du2013automatic,silva2018license}.

During deployment, data selection occurs dynamically, as image frames are captured in real time from an external camera connected to the NVIDIA Jetson Nano. Only frames containing vehicles or candidate license plates are forwarded to subsequent stages, ensuring computational efficiency and relevance of the extracted data.

\subsection{Data Preprocessing and Cleaning}

In both training and deployment stages, preprocessing and cleaning play a central role in improving data quality. During training, images are loaded using the OpenCV library and undergo resizing, normalization, and color space conversion to conform to the input requirements of the YOLO-based detection model \cite{bradski2000opencv,opencv_wiki}. Noisy or low-quality samples are either corrected or excluded to prevent degradation of the learning process.

To enhance generalization and mitigate overfitting, data augmentation techniques such as geometric transformations and brightness variation are applied during training \cite{shorten2019survey}. During deployment, preprocessing ensures consistency between live camera input and the data distribution observed during training, which is essential for maintaining reliable inference performance on embedded platforms.

\subsection{Data Transformation}

The transformation stage converts preprocessed data into representations suitable for knowledge extraction. During training, annotated images are transformed into structured input-output pairs consisting of pixel data and corresponding bounding box labels. These representations enable supervised learning of spatial and semantic patterns associated with license plates \cite{redmon2016you}.

In the deployment phase, transformation occurs through inference. Raw image frames are converted into bounding box predictions by the trained YOLO model, effectively transforming unstructured pixel data into structured object-level information \cite{bochkovskiy2020yolov4}. Detected license plate regions are further transformed into alphanumeric text using optical character recognition, bridging the gap between visual perception and symbolic data representation \cite{jaidedai2020easyocr}.

\subsection{Data Mining}

Data mining represents the core analytical stage of the KDD process, where algorithms extract patterns or models from transformed data \cite{han2011data}. In the training phase, data mining is realized through deep learning model optimization. The YOLO detection model learns hierarchical visual features that distinguish license plates from background elements by iteratively minimizing a loss function over the training dataset.

This learning process embodies pattern discovery at scale, as the model encodes complex spatial and contextual relationships within its parameters \cite{lecun2015deep}. In the deployment phase, the trained models continue to perform data mining in an online manner, applying learned patterns to incoming image streams to detect license plates and recognize characters in real time.

\subsection{Interpretation and Evaluation}

Interpretation and evaluation occur at multiple stages of the KDD pipeline. During training, model performance is evaluated using standard object detection and recognition metrics, such as precision, recall, and qualitative inspection of predicted bounding boxes \cite{everingham2010pascal}. These evaluations guide hyperparameter tuning and model selection, ensuring that the discovered patterns generalize beyond the training dataset.

During deployment, interpretation focuses on the correctness and consistency of extracted license plate numbers and timestamps. Detection errors, OCR inaccuracies, and duplicate records are analyzed and filtered where necessary to maintain data integrity. System-level performance metrics, including inference latency and reliability on the Jetson Nano, are also evaluated to ensure operational feasibility.

\subsection{Knowledge Presentation and Storage}

The final stage of the KDD process involves presenting and storing extracted knowledge in a form suitable for practical use. In the deployment phase, recognized license plate numbers are paired with entry and exit timestamps and stored in a SQLite database. SQLite is well suited for this task due to its lightweight architecture, transactional support, and compatibility with embedded systems \cite{sqlite_doc}.

Higher-level knowledge is subsequently derived by computing temporal differences between entry and exit events and applying predefined pricing rules to calculate parking fees. This transformation of low-level visual observations into actionable financial information represents the culmination of the KDD process, enabling autonomous decision-making at the edge without reliance on cloud-based infrastructure \cite{shi2016edge}.

\noindent
By consolidating both the training and operational stages within the KDD framework, this methodology demonstrates how knowledge discovery extends beyond post-deployment analytics and encompasses the entire lifecycle of a data-driven computer vision system.

% --------------------------------------------------------

% --------------------------------------------------------
\section{Methodology Justification: Knowledge Discovery in Databases (KDD)}
\index{Methodology!Justification}

\noindent
The Knowledge Discovery in Databases (KDD) process was selected as the methodological foundation of this project due to its ability to provide a structured, end-to-end framework for transforming raw data into actionable knowledge. Unlike narrowly scoped machine learning workflows that emphasize only model training or inference, KDD explicitly captures the full lifecycle of data-driven systems, encompassing data selection, preprocessing, transformation, mining, interpretation, and knowledge presentation \cite{fayyad1996knowledge,han2011data}. This makes KDD particularly well suited for complex computer vision applications that operate across both offline and real-time stages.\newline

\noindent
A key justification for adopting KDD lies in its alignment with the dual-phase nature of the license plate detection system. The project involves both an offline training phase, where knowledge is learned from historical visual data, and an online deployment phase, where new knowledge is continuously extracted from live camera streams on embedded hardware. KDD naturally accommodates this separation while maintaining conceptual continuity, allowing learned patterns to be systematically transferred from training to operational inference \cite{shearer2000crisp}. By treating deployment as a continuation of the knowledge discovery process rather than a terminal step, KDD enables consistent reasoning about data quality, transformations, and outcomes throughout the system lifecycle.\newline

\noindent
Furthermore, KDD emphasizes the importance of data quality and representation before model learning occurs. In computer vision systems, raw image data is inherently noisy and highly variable due to changes in illumination, viewpoint, background clutter, and sensor limitations. The explicit preprocessing and transformation stages of KDD provide a principled justification for image normalization, augmentation, and feature representation, which are essential for reliable pattern discovery in visual domains \cite{du2013automatic,lecun2015deep}. This focus reduces the risk of treating model performance as an isolated artifact and instead frames it as the result of cumulative data refinement decisions.\newline

\noindent
Another important justification for using KDD is its emphasis on interpretation and evaluation as integral components of the discovery process. In the context of license plate recognition, extracted knowledge must be not only accurate but also meaningful and reliable when applied to downstream tasks such as timestamp logging and fee computation. KDD explicitly requires that discovered patterns be evaluated, interpreted, and validated against domain expectations, which supports systematic error analysis, filtering of incorrect detections, and validation of system behavior under real-world conditions \cite{everingham2010pascal}. This is particularly critical for embedded systems, where incorrect outputs can propagate directly into automated decision-making.\newline

\noindent
Finally, KDD is justified by its strong focus on knowledge presentation and storage. The ultimate goal of the proposed system is not merely detection accuracy, but the generation of structured, persistent knowledge in the form of recognized license plates, temporal records, and computed fees. KDD provides a clear rationale for transforming low-level visual observations into higher-level symbolic and transactional data suitable for database storage and subsequent querying \cite{fayyad1996knowledge,han2011data}. This ensures that the system output is actionable, auditable, and extensible, rather than remaining confined to transient model predictions.\newline

\noindent
Overall, the adoption of the KDD methodology provides a rigorous and holistic justification for the design of the proposed license plate detection system. By framing both training and deployment within a unified knowledge discovery paradigm, KDD ensures methodological consistency, interpretability, and practical relevance, making it well suited for license plate detection using Jetson Nano.\newline